import useTheme from "./lib/useTheme";
import {
  LOCAL_STORAGE_THEME_KEY,
  Theme,
  ThemeContext,
} from "entities/theme/lib/ThemeContext";

export { Theme, LOCAL_STORAGE_THEME_KEY, ThemeContext, useTheme };
